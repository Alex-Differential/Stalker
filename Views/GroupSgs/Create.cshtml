@model StalkerApplication.GroupSg

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>GroupSg</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="GsGrid" class="control-label"></label>
                <select asp-for="GsGrid" class ="form-control" asp-items="ViewBag.GsGrid"></select>
            </div>
            <div class="form-group">
                <label asp-for="GsSgid" class="control-label"></label>
                <select asp-for="GsSgid" class ="form-control" asp-items="ViewBag.GsSgid"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
