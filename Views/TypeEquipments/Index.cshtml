@model IEnumerable<StalkerApplication.TypeEquipment>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Додати тип екіпірування</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TeName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TeName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TeId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TeId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TeId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
